#include <stdio.h>
#include<math.h>
int main()
{
	double n, m;
	scanf_s("%d %d", &n, &m);
	int num_arr[101] = { 0, };
	n = sqrt(n)+1;
	m = sqrt(m);
	int sum, min;
	for (double i = (int)n; i < b; i+=1)
	{
		sum += pow(i, 2);
	}
	min = pow((int)n, 2);
	printf("%d\n%d", sum.min);0


	return 0;
}